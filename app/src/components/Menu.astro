---
import Menu from '@/components/Menu.tsx';
import Header from '@/components/Header.astro';

const pathname = Astro.url.pathname;
---

{pathname !== '/' && pathname !== '/save-the-date' && <Header />}
<Menu client:load pathname={pathname} />
